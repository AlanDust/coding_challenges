source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.3.3'
gem 'nokogiri'
gem 'httparty'
gem 'byebug'

group :development, :test do
  gem 'jasmine'
end
